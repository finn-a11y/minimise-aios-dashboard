import React from 'react';
import PagePlaceholder from './PagePlaceholder';

export default function AiosSkills() {
  return (
    <PagePlaceholder
      title="Skills"
      description="Skill catalog grid + paginated activity log. Wired in Stream 3."
    />
  );
}
