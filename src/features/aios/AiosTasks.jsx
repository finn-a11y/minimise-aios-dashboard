import React from 'react';
import PagePlaceholder from './PagePlaceholder';

export default function AiosTasks() {
  return (
    <PagePlaceholder
      title="Tasks"
      description="Task ledger grouped by department × category. Wired in Stream 3."
    />
  );
}
