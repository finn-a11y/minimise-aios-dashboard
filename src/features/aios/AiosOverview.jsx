import React from 'react';
import PagePlaceholder from './PagePlaceholder';

export default function AiosOverview() {
  return (
    <PagePlaceholder
      title="Overview"
      description="Hero KPIs, department grid, recent runs activity feed. Wired in Stream 3."
    />
  );
}
